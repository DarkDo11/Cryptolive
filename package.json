{
  "name": "cryptolive",
  "version": "2.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node server/server.js",
    "dev": "node --watch server/server.js",
    "test": "node scripts/check.mjs && node --test test/*.test.js",
    "check": "node scripts/check.mjs"
  }
}
